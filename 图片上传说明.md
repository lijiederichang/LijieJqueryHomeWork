# 图片上传功能说明

## 当前实现

### 前端功能
1. ✅ 文件选择器 - 用户可以从本地选择图片文件
2. ✅ 图片预览 - 选择图片后立即显示预览
3. ✅ 文件验证 - 验证文件类型和大小
4. ✅ 路径生成 - 生成图片存储路径格式：`images/card_{timestamp}.{extension}`

### 存储方式

#### 当前（前端模拟）
- 图片路径存储在 `localStorage` 的 JSON 数据中
- 格式：`images/card_1702567890123.jpg`
- 实际文件需要手动放置到 `images/` 目录

#### 生产环境（需要后端支持）
- 图片文件通过后端API上传到服务器
- 文件保存在服务器的 `images/` 目录
- 图片路径存储在 MySQL 数据库的 `cards` 表中

## 图片路径示例

项目中使用的图片路径格式：

```
images/card_1702567890123.jpg
images/card_1702567890124.png
images/game1.jpg
images/game2.jpg
```

## 如何添加示例图片

1. 将图片文件复制到 `images/` 目录
2. 确保文件名符合命名规则或使用示例文件名
3. 在代码中引用路径，例如：`image: "images/game1.jpg"`

## 示例图片文件

为了演示游戏分类，建议在 `images/` 目录中放置以下图片：

- `game1.jpg` - 电子竞技相关图片
- `game2.jpg` - 游戏开发相关图片

## 后端集成（生产环境）

如果需要实际的文件上传功能，需要：

1. **后端API** - 创建文件上传接口
   ```
   POST /api/upload
   Content-Type: multipart/form-data
   ```

2. **文件处理** - 后端接收文件并保存到 `images/` 目录

3. **数据库存储** - 将图片路径保存到 MySQL 数据库

4. **前端修改** - 修改 `script.js` 中的 `submitCardForm()` 函数，使用实际的后端API地址

## 当前代码说明

在 `script.js` 的 `submitCardForm()` 函数中：

```javascript
// 模拟Ajax上传 - 实际项目中需要后端支持
$.ajax({
    url: '/api/upload', // 需要修改为实际的后端API地址
    type: 'POST',
    data: formDataObj,
    processData: false,
    contentType: false,
    ...
});
```

**注意**：当前代码会模拟上传成功，实际文件需要手动处理或集成后端服务。

## 本地测试

1. 手动将图片文件放到 `images/` 目录
2. 在添加卡片时，系统会生成路径但不会实际上传文件
3. 路径会保存在 localStorage 中
4. 显示时使用相对路径访问图片

## MySQL 存储

图片路径在 MySQL 数据库中的存储方式：

```sql
-- cards 表中的 image_path 字段
image_path VARCHAR(500) NOT NULL DEFAULT '' COMMENT '图片存储路径'

-- 示例数据
INSERT INTO cards (title, image_path, ...) VALUES 
('卡片标题', 'images/card_1702567890123.jpg', ...);
```

前端显示时：
```html
<img src="images/card_1702567890123.jpg" alt="卡片标题">
```





