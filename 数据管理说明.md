# 📦 数据管理说明

## 🎯 数据分类和存储策略

项目采用**双数据源**策略，区分示例数据和用户数据：

### 1. 示例数据（ID: 1001-1020）

**特点**：
- 内置的20条演示数据
- 存储在 `js/demo-data.js`
- **使用前端localStorage进行增删改查**
- **不调用后端API**

**存储位置**：
- 原始数据：`js/demo-data.js`
- 修改记录：`localStorage`（键名：`demoCardsData`）

**功能**：
- ✅ 添加：不支持（示例数据固定）
- ✅ 查询：从 `demo-data.js` 加载，合并localStorage中的修改
- ✅ 更新：保存到localStorage，不调用后端
- ✅ 删除：标记为已删除（localStorage），刷新页面会恢复

### 2. 用户数据（ID: < 1000）

**特点**：
- 用户通过表单添加的数据
- **保存到MySQL数据库**
- **通过后端API进行增删改查**

**存储位置**：
- MySQL数据库（`cards` 表）
- 后端API管理

**功能**：
- ✅ 添加：调用 `POST /api/cards` 保存到MySQL
- ✅ 查询：调用 `GET /api/cards` 从MySQL加载
- ✅ 更新：调用 `POST /api/cards/{id}/update` 更新MySQL
- ✅ 删除：调用 `DELETE /api/cards/{id}` 从MySQL删除

## 🔧 实现细节

### 示例数据管理（`js/demo-storage.js`）

```javascript
// 获取示例数据（合并localStorage中的修改）
getDemoCards()

// 更新示例数据（保存到localStorage）
updateDemoCard(cardId, cardData)

// 删除示例数据（标记为已删除）
deleteDemoCard(cardId)

// 检查是否是示例数据
isDemoCard(cardId)  // ID: 1001-1020
```

### 用户数据管理（后端API）

```javascript
// 添加：POST /api/cards
// 查询：GET /api/cards
// 更新：POST /api/cards/{id}/update
// 删除：DELETE /api/cards/{id}
```

## 📊 数据流程

### 页面加载流程

1. **加载示例数据**
   - 从 `demo-data.js` 读取原始数据
   - 从 `localStorage` 读取修改记录
   - 合并显示

2. **加载用户数据**
   - 尝试连接后端API
   - 如果成功：从MySQL加载用户数据
   - 如果失败：只显示示例数据

3. **合并显示**
   - 示例数据 + 用户数据
   - 按创建时间倒序排列

### 添加卡片流程

1. 用户填写表单，上传图片
2. **调用后端API**：`POST /api/cards`
3. 保存到MySQL数据库
4. 如果后端不可用，提示错误

### 编辑卡片流程

**示例数据（ID: 1001-1020）**：
1. 修改保存到localStorage
2. 不调用后端API
3. 刷新页面后修改保持

**用户数据（ID: < 1000）**：
1. 调用后端API：`POST /api/cards/{id}/update`
2. 更新MySQL数据库
3. 如果后端不可用，提示错误

### 删除卡片流程

**示例数据（ID: 1001-1020）**：
1. 标记为已删除（localStorage）
2. 从显示中移除
3. 刷新页面后会恢复（因为原始数据在 `demo-data.js`）

**用户数据（ID: < 1000）**：
1. 调用后端API：`DELETE /api/cards/{id}`
2. 从MySQL删除
3. 如果后端不可用，提示错误

## ⚠️ 重要说明

### 1. 示例数据的特点

- **可编辑**：可以修改示例数据，修改保存在localStorage
- **可删除**：可以删除示例数据，删除标记保存在localStorage
- **可恢复**：刷新页面后，示例数据会恢复（因为原始数据在代码中）
- **不持久**：清除浏览器数据会丢失所有修改

### 2. 用户数据的特点

- **需要后端**：必须启动后端服务才能添加/编辑/删除
- **持久存储**：数据保存在MySQL，不会丢失
- **跨设备**：不同设备访问同一后端，数据共享

### 3. 后端不可用时的行为

- **示例数据**：正常显示，可以增删改查（使用localStorage）
- **用户数据**：
  - 添加：提示后端不可用
  - 编辑：提示后端不可用
  - 删除：提示后端不可用
  - 查询：只显示示例数据

## 🎯 使用场景

### 场景1：仅前端（无后端）

- ✅ 显示20条示例数据
- ✅ 可以编辑示例数据（修改保存在localStorage）
- ✅ 可以删除示例数据（删除标记保存在localStorage）
- ❌ 无法添加新卡片（需要后端）
- ❌ 无法编辑/删除用户数据（需要后端）

### 场景2：完整后端

- ✅ 显示示例数据 + 用户数据
- ✅ 可以编辑示例数据（localStorage）
- ✅ 可以删除示例数据（localStorage）
- ✅ 可以添加新卡片（保存到MySQL）
- ✅ 可以编辑用户数据（更新MySQL）
- ✅ 可以删除用户数据（从MySQL删除）

## 📝 代码文件说明

- **`js/demo-data.js`**：示例数据定义
- **`js/demo-storage.js`**：示例数据localStorage管理
- **`js/api.js`**：数据加载和合并逻辑
- **`js/form.js`**：表单提交（区分示例数据和用户数据）
- **`js/utils.js`**：删除功能（区分示例数据和用户数据）

---

**总结**：示例数据使用前端localStorage，用户数据使用后端MySQL，两者互不干扰，完美配合！

