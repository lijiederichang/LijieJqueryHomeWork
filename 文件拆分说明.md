# script.js 文件拆分说明

## 拆分完成 ✅

原来的 `script.js` 文件（1159行）已成功拆分为 **11个模块化文件**，便于维护和管理。

## 拆分后的文件结构

```
js/
├── config.js          # 配置和全局变量（15行）
├── api.js             # API相关函数（77行）
├── card.js            # 卡片相关功能（158行）
├── like.js            # 点赞功能（47行）
├── form.js            # 表单处理（245行）
├── detail.js          # 详情页功能（89行）
├── category.js        # 分类页功能（79行）
├── favorites.js       # 我的喜欢页面（78行）
├── utils.js           # 工具函数（78行）
├── index.js           # 首页功能（50行）
├── main.js            # 主入口文件（28行）
└── README.md          # 文件结构说明
```

## 文件大小对比

- **拆分前**: `script.js` - 1159行（单个大文件）
- **拆分后**: 11个文件，平均每个约100行（模块化）

## 各文件功能说明

### 1. config.js
- API基础URL配置
- 全局变量定义（cardsData, userId）

### 2. api.js
- `loadCardsFromAPI()` - 从后端加载数据
- `loadLikeStatuses()` - 加载点赞状态
- `loadCardsFromStorage()` - 本地存储加载
- `saveCardsToStorage()` - 本地存储保存

### 3. card.js
- `createCardElement()` - 创建卡片DOM元素
- `renderCards()` - 渲染卡片列表
- `performSearch()` - 搜索功能
- `performFilter()` - 筛选功能

### 4. like.js
- `toggleLike()` - 点赞/取消点赞

### 5. form.js
- `initAddCardPage()` - 添加卡片页面
- `initEditCardPage()` - 编辑卡片页面
- `handleImagePreview()` - 图片预览
- `submitCardForm()` - 提交表单
- `updateCardForm()` - 更新表单
- `validateField()` - 表单验证
- `showFormMessage()` - 表单消息

### 6. detail.js
- `initDetailPage()` - 详情页初始化
- `renderCardDetail()` - 渲染详情

### 7. category.js
- `initCategoryPage()` - 分类页初始化
- `filterCardsByCategory()` - 分类筛选
- `renderCategoryCards()` - 渲染分类卡片

### 8. favorites.js
- `initFavoritesPage()` - 我的喜欢页面初始化
- `loadFavorites()` - 加载喜欢的卡片

### 9. utils.js
- `initBackToTop()` - 返回顶部
- `showToast()` - 提示消息
- `deleteCard()` - 删除卡片

### 10. index.js
- `initIndexPage()` - 首页初始化

### 11. main.js
- 主入口，路由分发

## HTML文件更新

所有HTML文件已更新，按需加载相应的JS文件：

- **index.html**: config + api + card + like + utils + index + main
- **add-card.html**: config + api + form + utils + main
- **detail.html**: config + api + card + like + detail + utils + main
- **category.html**: config + api + card + like + category + utils + main
- **favorites.html**: config + api + card + like + favorites + utils + main
- **edit-card.html**: config + api + form + utils + main
- **about.html**: config + api + utils + main

## 优势

1. ✅ **模块化**: 每个文件职责单一
2. ✅ **可维护性**: 修改功能只需编辑对应文件
3. ✅ **可读性**: 文件变小，更容易理解
4. ✅ **可扩展性**: 新增功能只需添加新文件
5. ✅ **按需加载**: 每个页面只加载需要的文件

## 注意事项

1. **加载顺序**: 必须按照依赖关系顺序加载
2. **全局变量**: 在config.js中定义，所有文件共享
3. **向后兼容**: 功能完全保持不变
4. **原文件保留**: `script.js` 文件保留作为备份

## 使用说明

1. 所有功能保持不变
2. 只需确保JS文件按正确顺序加载
3. 如需修改某个功能，找到对应的JS文件即可
4. 新增功能时，创建新的JS文件并在HTML中引用

拆分完成！代码结构更清晰，维护更方便！




