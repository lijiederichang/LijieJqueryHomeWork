# 📦 演示数据说明

## 🎯 功能说明

项目已内置 **20条演示数据**，即使在没有后端服务器的情况下，网站也能正常显示内容。

## ✨ 特点

- ✅ **20条精选卡片数据**，涵盖所有6个分类
- ✅ **使用在线CDN图片**（Unsplash），无需本地图片文件
- ✅ **自动检测后端连接**，失败时自动切换到演示模式
- ✅ **点赞功能正常**，使用localStorage保存点赞状态
- ✅ **所有功能可用**，搜索、筛选、详情页等都能正常工作

## 📊 数据分布

演示数据包含以下分类：

- **科技类**：4条（人工智能、量子计算、5G网络、虚拟现实）
- **艺术类**：4条（抽象艺术、印象派、现代雕塑、街头艺术）
- **自然类**：3条（山川、海洋、森林）
- **运动类**：3条（篮球、足球、网球）
- **音乐类**：3条（古典音乐、爵士乐、摇滚音乐节）
- **游戏类**：3条（电子竞技、开放世界、策略游戏）

## 🔧 工作原理

### 1. 自动检测模式

当页面加载时，系统会：

1. **尝试连接后端API**（3秒超时）
2. **如果连接成功**：使用后端数据
3. **如果连接失败**：自动切换到演示数据模式

### 2. 演示数据加载

```javascript
// 自动加载演示数据
loadDemoData(callback);
```

演示数据会：
- 从 `js/demo-data.js` 加载20条数据
- 从 `localStorage` 恢复点赞状态
- 正常显示所有卡片

### 3. 点赞功能

在演示模式下：
- 点赞状态保存在 `localStorage` 中
- 点赞数会实时更新
- 刷新页面后状态保持

## 📁 文件说明

### `js/demo-data.js`

包含：
- `DEMO_CARDS_DATA`：20条演示数据数组
- `loadDemoLikeStatuses()`：加载点赞状态
- `saveDemoLikeStatus()`：保存点赞状态

### 修改的文件

1. **`js/api.js`**：添加演示数据加载逻辑
2. **`js/like.js`**：支持演示数据的点赞功能
3. **`js/card.js`**：修复CDN图片显示
4. **`js/detail.js`**：支持演示数据详情页
5. **所有HTML文件**：引入 `demo-data.js`

## 🚀 使用场景

### 场景1：GitHub Pages部署

部署到GitHub Pages后，由于没有后端服务器：
- ✅ 自动使用演示数据
- ✅ 所有功能正常
- ✅ 图片从CDN加载

### 场景2：本地开发（无后端）

如果后端服务器未启动：
- ✅ 自动切换到演示模式
- ✅ 可以测试所有前端功能
- ✅ 不影响开发体验

### 场景3：有后端服务器

如果后端正常运行：
- ✅ 优先使用后端数据
- ✅ 演示数据作为备用
- ✅ 无缝切换

## 🔍 如何识别演示数据

演示数据的ID范围：**1001-1020**

- 如果看到卡片ID >= 1000，说明是演示数据
- 如果看到卡片ID < 1000，说明是后端数据

## 📝 自定义演示数据

如果需要修改演示数据，编辑 `js/demo-data.js` 文件：

```javascript
const DEMO_CARDS_DATA = [
    {
        id: 1001,  // 唯一ID（建议 >= 1000）
        title: '标题',
        description: '描述',
        image: 'https://images.unsplash.com/...',  // CDN图片地址
        category: 'technology',  // 分类
        author: '作者名',
        likes: 100,  // 初始点赞数
        liked: false  // 是否已点赞
    },
    // ... 更多数据
];
```

## ⚠️ 注意事项

1. **图片地址**：建议使用CDN地址（如Unsplash），确保在线可访问
2. **ID范围**：演示数据ID建议 >= 1000，避免与后端数据冲突
3. **点赞状态**：演示数据的点赞状态保存在 `localStorage`，清除浏览器数据会重置
4. **数据更新**：修改 `demo-data.js` 后需要刷新页面才能看到效果

## 🎉 优势

- ✅ **零配置**：无需后端即可运行
- ✅ **完整功能**：所有功能都能正常使用
- ✅ **自动切换**：智能检测后端连接
- ✅ **易于演示**：适合展示和分享项目
- ✅ **开发友好**：前端开发不受后端影响

---

**提示**：如果后端服务器正常运行，系统会自动使用后端数据，演示数据作为备用方案。

