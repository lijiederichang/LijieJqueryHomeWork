# 🚀 GitHub Pages 部署命令速查卡

快速参考，适合已经熟悉流程的用户。

---

## 📋 完整部署流程（复制粘贴版）

### 1. 检查Git
```bash
git --version
```

### 2. 配置Git（首次使用）
```bash
git config --global user.name "你的名字"
git config --global user.email "你的邮箱@example.com"
```

**说明**：
- `user.name`：可以是你的真实姓名、GitHub用户名或任何标识名称
  - 这个名称会显示在Git提交历史中（告诉别人是谁提交的代码）
  - **不一定要和GitHub用户名相同**，但建议使用一致或相关的名称
  - 示例：`"张三"`、`"zhangsan"`、`"Zhang San"` 都可以
- `user.email`：建议使用GitHub账号邮箱（用于关联提交记录）
  - 如果不想公开邮箱，可以使用GitHub提供的noreply邮箱：
    - 格式：`USERNAME@users.noreply.github.com`
    - 例如：`zhangsan@users.noreply.github.com`

### 3. 进入项目目录
```bash
cd E:\LiJie-Jquery-Exam
```

### 4. 初始化Git仓库
```bash
git init
```

### 5. 添加所有文件
```bash
git add .
```

### 6. 提交代码
```bash
git commit -m "Initial commit: 交互式媒体卡片墙项目"
```

### 7. 添加远程仓库（替换YOUR_USERNAME和REPO_NAME）
```bash
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git
```

### 8. 设置主分支
```bash
git branch -M main
```

### 9. 推送到GitHub
```bash
git push -u origin main
```

---

## 🔄 更新代码流程

```bash
cd E:\LiJie-Jquery-Exam
git add .
git commit -m "更新描述"
git push origin main
```

---

## 🔍 常用检查命令

### 查看Git状态
```bash
git status
```

### 查看提交历史
```bash
git log
```

### 查看远程仓库
```bash
git remote -v
```

### 查看分支
```bash
git branch
```

---

## ⚠️ 常见错误处理

### 远程仓库已存在
```bash
git remote remove origin
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git
```

### 推送前需要拉取
```bash
git pull origin main --allow-unrelated-histories
git push -u origin main
```

### 撤销最后一次提交（未推送）
```bash
git reset --soft HEAD~1
```

---

## 📝 GitHub Pages 配置

1. 打开仓库 → **Settings** → **Pages**
2. Source: **main** branch
3. Folder: **/ (root)**
4. 点击 **Save**

---

## 🌐 网站地址格式

```
https://YOUR_USERNAME.github.io/REPO_NAME/
```

---

**详细说明请查看 `详细手动部署指南.md`**

